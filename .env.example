# Railway supplies PORT automatically. This default is useful when running locally.
PORT=8080

# Set both values as Railway service variables. Do not commit real secrets here.
HOST_PASSWORD=
HOST_SESSION_SECRET=
